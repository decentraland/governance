setup:
  addons:
    - plan: heroku-postgresql:mini
      as: DATABASE
build:
  docker:
#    release:
#      dockerfile: Dockerfile
#      target: compiler
    web: Dockerfile
#  config:
#    version_number: 99
#    heroku_app_name: heroku-app-name
