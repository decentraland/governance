import { NewProposalHiring } from '../types'

import { template } from './utils'

export const title = (proposal: NewProposalHiring) => ``

export const description = (proposal: NewProposalHiring) => template``
